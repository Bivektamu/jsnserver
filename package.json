{
  "name": "jsn_server",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm install && npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bivek",
  "license": "ISC",
  "devDependencies": {
    "express": "^4.18.2"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
